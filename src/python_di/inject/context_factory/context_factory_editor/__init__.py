# 1. Merge together all factories of the same type by profile, so have per profile per factory type
# 2. Merge together all factories of same profile, so only have one profile. Keep unique injection metadata